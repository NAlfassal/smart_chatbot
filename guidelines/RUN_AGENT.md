# 🚀 كيف تشغل الـ Agent الآن

## الخطوات (5 دقائق فقط!)

### 1️⃣ تثبيت المكتبات المطلوبة

```bash
pip install langgraph langchain-anthropic beautifulsoup4 lxml python-dotenv httpx
```

---

### 2️⃣ إعداد مفتاح Anthropic API

#### طريقة أ: ملف .env (الأسهل)

1. أنشئ ملف اسمه `.env` في نفس المجلد:

```bash
# في Windows (PowerShell):
New-Item -Path .env -ItemType File

# أو في CMD:
type nul > .env
```

2. افتح الملف واكتب فيه:

```
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxx
```

(استبدل بمفتاحك الحقيقي من https://console.anthropic.com/)

#### طريقة ب: متغير بيئة مؤقت

**Windows PowerShell:**
```powershell
$env:ANTHROPIC_API_KEY="sk-ant-api03-xxxxxxxxxxxxxxx"
```

**Windows CMD:**
```cmd
set ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxx
```

---

### 3️⃣ شغّل الـ Agent!

```bash
python latest_agent.py
```

---

## ✅ ماذا سيحدث؟

سترى:
```
🚀 إنشاء Agent للبحث في موقع SFDA...
✅ تم إنشاء Agent بنجاح!

📝 أمثلة على الاستخدام:

============================================================
مثال 1: مرحباً، أريد البحث عن دواء
============================================================

🤖 الرد:
[رد Agent]

============================================================
مثال 2: هل يمكنك إعطائي معلومات عن موقع الهيئة؟
============================================================

🤖 الرد:
[رد Agent مع معلومات الموقع]

... إلخ
```

---

## 🎮 تجربة تفاعلية

إذا تريد تجربة Agent بشكل تفاعلي، استخدم هذا الكود:

```python
# أنشئ ملف: test_agent.py

from latest_agent import create_sfda_agent, chat_with_agent

print("🤖 Agent جاهز! اكتب 'خروج' للإنهاء\n")

agent = create_sfda_agent()
session_id = "test_session"

while True:
    user_input = input("\n👤 أنت: ")

    if user_input.lower() in ['خروج', 'exit', 'quit']:
        print("👋 مع السلامة!")
        break

    response = chat_with_agent(agent, user_input, thread_id=session_id)
    print(f"\n🤖 Agent: {response}")
```

ثم شغّله:
```bash
python test_agent.py
```

---

## ⚠️ استكشاف الأخطاء

### خطأ: ModuleNotFoundError: No module named 'langgraph'

**الحل:**
```bash
pip install langgraph
```

---

### خطأ: API key not found

**الحل:**
تأكد من إعداد المفتاح بإحدى الطريقتين أعلاه

---

### خطأ: Connection timeout عند البحث في SFDA

**هذا طبيعي!** لأن الكود الحالي يتحقق فقط من توفر الموقع.

للبحث الفعلي، راجع [upgrade_to_playwright.md](upgrade_to_playwright.md)

---

## 🎯 ما يمكنك تجربته

1. **سؤال عام:**
   ```
   "مرحباً"
   ```

2. **طلب معلومات:**
   ```
   "أعطني معلومات عن موقع الهيئة"
   ```

3. **البحث عن دواء:**
   ```
   "ابحث عن دواء برقم التسجيل 12345"
   ```
   (سيخبرك أن البحث الكامل يحتاج Playwright)

---

## 📊 الأوامر السريعة

| الأمر | الوصف |
|-------|--------|
| `python latest_agent.py` | تشغيل الأمثلة الافتراضية |
| `python test_agent.py` | تشغيل نسخة تفاعلية |
| `pip list \| grep lang` | التحقق من المكتبات المثبتة |

---

**جاهز؟ جرّب الآن!** 🎉
