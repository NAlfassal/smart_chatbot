# خطة واقعية - 3 أيام فقط
**المشروع:** SFDA Legal Assistant MVP
**الهدف:** إنجاز المتطلبات الأساسية فقط

---

## 🎯 المتطلبات الأساسية من المشروع الأصلي

من ملف `01 - The Saudi Legal Assistant.md`:

| المطلوب | الحالة | الأولوية |
|---------|--------|----------|
| ✅ Web Scraper + Vector DB | **مُنجز** | - |
| ✅ RAG Pipeline بالعربية | **مُنجز** | - |
| ✅ Chat Interface | **مُنجز** | - |
| ⚠️ Article Citations واضحة | يحتاج تحسين | **P0** |
| ⚠️ Guardrails (رفض أسئلة غير قانونية) | غير موجود | **P0** |
| 🔄 Testing | جزئي | **P1** |
| 🔄 Documentation للتسليم | جزئي | **P1** |

---

## ⚡ الخطة المبسطة (3 أيام)

```
┌──────────┬────────────────────────────────────┬─────────┐
│  اليوم   │            المهام                 │  الوقت  │
├──────────┼────────────────────────────────────┼─────────┤
│  Day 1   │ تحسين Article Citations +         │ 4 ساعات │
│          │ إضافة Guardrails                  │         │
├──────────┼────────────────────────────────────┼─────────┤
│  Day 2   │ Testing + Bug Fixes                │ 4 ساعات │
├──────────┼────────────────────────────────────┼─────────┤
│  Day 3   │ Documentation + Deployment Prep    │ 3 ساعات │
└──────────┴────────────────────────────────────┴─────────┘
```

---

## 📅 Day 1: التحسينات الضرورية (4 ساعات)

### ✅ Task 1: تحسين Article Citations (1.5 ساعة)

**المشكلة:** الردود الحالية لا تُظهر رقم المادة بوضوح كافٍ

**الحل:**

تعديل `app_gradio.py` - وظيفة `build_knowledge`:

```python
def build_knowledge(docs) -> str:
    """بناء المعرفة مع إظهار أرقام المواد"""
    parts = []
    for d in docs:
        src = display_source_name(d.metadata.get("source", "N/A"))
        snippet = pretty_arabic_text(d.page_content)[:1400]

        # ✅ NEW: إضافة رقم المادة إذا موجود
        article_num = d.metadata.get("article")
        if article_num:
            parts.append(f"[{src} - المادة {article_num}]\n{snippet}")
        else:
            parts.append(f"[{src}]\n{snippet}")

    return "\n\n".join(parts)
```

تعديل الـ `generation_prompt`:

```python
generation_prompt = f"""
ROLE:
أنت مساعد قانوني متخصص في لوائح هيئة الغذاء والدواء السعودية.

RULES:
1. اعتمد فقط على النصوص المرفقة
2. **اذكر رقم المادة دائماً** عند الإجابة (مثال: "حسب المادة 4...")
3. إذا لم تجد نصاً صريحاً، قل: "لم أجد نصاً صريحاً"
4. لا تقدم مشورة قانونية - فقط اذكر ما ورد في النص

IMPORTANT:
- ابدأ إجابتك بـ "حسب المادة X من لوائح التجميل..."
- إذا كان من عدة مواد، اذكر كل الأرقام

النصوص المساعدة:
{knowledge}

سؤال المستخدم: {message}

الإجابة:
""".strip()
```

---

### ✅ Task 2: إضافة Guardrails (1.5 ساعة)

**المطلوب:** رفض الأسئلة غير القانونية + إضافة disclaimer

**الحل:**

إضافة في بداية `app_gradio.py`:

```python
# =====================================
# Guardrails Configuration
# =====================================

FORBIDDEN_TOPICS = [
    # أسئلة عامة ليست قانونية
    r"ما هو الطقس",
    r"من أنت",
    r"كيف حالك",
    r"ساعدني في",

    # طلبات مشورة قانونية مباشرة
    r"هل يجب علي",
    r"هل أستطيع",
    r"ماذا أفعل",
    r"كيف أتصرف",
]

LEGAL_ADVICE_PATTERNS = [
    r"ما رأيك",
    r"انصحني",
    r"ما هو الأفضل",
]

def check_guardrails(query: str) -> tuple[bool, str | None]:
    """
    Returns: (is_allowed, error_message)
    """
    query_lower = query.lower()

    # ✅ 1. رفض أسئلة غير قانونية
    for pattern in FORBIDDEN_TOPICS:
        if re.search(pattern, query, re.IGNORECASE):
            return False, """
⚠️ عذراً، أنا متخصص فقط في الإجابة عن أسئلة تتعلق بـ:
- لوائح هيئة الغذاء والدواء السعودية
- المواد المحظورة في التجميل
- الأنظمة والتشريعات ذات الصلة

يرجى طرح سؤال قانوني محدد.
            """.strip()

    # ✅ 2. رفض طلبات المشورة القانونية
    for pattern in LEGAL_ADVICE_PATTERNS:
        if re.search(pattern, query, re.IGNORECASE):
            return False, """
⚠️ تنويه مهم:
لا أستطيع تقديم مشورة قانونية. أنا مساعد يعرض **فقط** ما ورد في النصوص الرسمية.

للحصول على استشارة قانونية:
- تواصل مع محامٍ مختص
- أو راجع الجهات المختصة مباشرة

يمكنني فقط عرض نصوص اللوائح والأنظمة.
            """.strip()

    return True, None

# ✅ إضافة disclaimer ثابت
DISCLAIMER = """

---
⚠️ **تنويه قانوني:**
- هذه المعلومات للإطلاع فقط وليست مشورة قانونية
- للحصول على رأي قانوني ملزم، راجع محامٍ مختص
- الموقع الرسمي: https://www.sfda.gov.sa
""".strip()
```

تعديل `stream_response`:

```python
def stream_response(message, history, source_choice):
    message = (message or "").strip()
    if not message:
        yield "اكتبي سؤالك."
        return

    # ✅ NEW: فحص Guardrails
    is_allowed, error_msg = check_guardrails(message)
    if not is_allowed:
        yield error_msg
        return

    # ... باقي الكود كما هو ...

    # ✅ في النهاية، أضف disclaimer
    final_answer = final_answer.strip()
    final_answer += sources_footer_once(top_docs, source_choice)
    final_answer += DISCLAIMER  # ← NEW

    yield final_answer
```

---

### ✅ Task 3: Testing Quick (1 ساعة)

إنشاء `test_basic.py`:

```python
# test_basic.py
"""اختبارات أساسية للـ MVP"""

def test_article_citation():
    """تحقق من ظهور رقم المادة"""
    # TODO: نفذ بعد التعديلات
    pass

def test_guardrails_non_legal():
    """تحقق من رفض أسئلة غير قانونية"""
    from app_gradio import check_guardrails

    is_allowed, msg = check_guardrails("ما هو الطقس؟")
    assert not is_allowed
    assert "متخصص فقط" in msg

    print("✅ Guardrails working!")

def test_guardrails_legal_advice():
    """تحقق من رفض طلبات المشورة"""
    from app_gradio import check_guardrails

    is_allowed, msg = check_guardrails("ما رأيك في هذا؟")
    assert not is_allowed
    assert "مشورة قانونية" in msg

    print("✅ Legal advice rejection working!")

if __name__ == "__main__":
    test_guardrails_non_legal()
    test_guardrails_legal_advice()
    print("\n✅ All tests passed!")
```

Run:
```bash
python test_basic.py
```

---

## ✅ End of Day 1 Checklist

- [ ] Article citations واضحة في كل إجابة
- [ ] Guardrails يرفض أسئلة غير قانونية
- [ ] Disclaimer ظاهر في كل رد
- [ ] الاختبارات الأساسية تمر ✅

---

## 📅 Day 2: Testing & Bug Fixes (4 ساعات)

### Morning: Manual Testing (2 ساعات)

#### اختبار شامل للـ UI:

**Test Cases:**

| # | السؤال | النتيجة المتوقعة |
|---|---------|------------------|
| 1 | "ما هي المادة 4؟" | ✅ يُظهر نص المادة + رقمها + disclaimer |
| 2 | "ما الطقس اليوم؟" | ✅ يرفض + رسالة واضحة |
| 3 | "انصحني ماذا أفعل" | ✅ يرفض + "لا أقدم مشورة" |
| 4 | "هل Mercury محظور؟" | ✅ يبحث في المحظورات + يُظهر المصدر |
| 5 | سؤال طويل جداً (>500 حرف) | ✅ يُظهر خطأ واضح |

**تسجيل الأخطاء:**
```
أنشئ ملف: bugs_found.txt
سجّل كل خطأ تجده
```

---

### Afternoon: Bug Fixes (2 ساعات)

#### أصلح الأخطاء المكتشفة

الأخطاء المتوقعة:
- رقم المادة لا يظهر في بعض الحالات
- Guardrails يرفض أسئلة صحيحة
- Formatting مكسور في بعض الردود

---

## ✅ End of Day 2 Checklist

- [ ] كل test cases تمر ✅
- [ ] لا توجد أخطاء واضحة
- [ ] UI يعمل بسلاسة
- [ ] Guardrails متوازن (لا يرفض أسئلة صحيحة)

---

## 📅 Day 3: Documentation & Delivery (3 ساعات)

### Task 1: تحديث README.md (1 ساعة)

```markdown
# SFDA Legal Assistant - MVP

## 📋 الوصف
مساعد قانوني متخصص في لوائح هيئة الغذاء والدواء السعودية.

## ✅ المميزات
- ✅ البحث الدلالي في لوائح SFDA
- ✅ عرض أرقام المواد بوضوح
- ✅ Guardrails (رفض أسئلة غير قانونية)
- ✅ Disclaimer قانوني في كل رد
- ✅ واجهة عربية سهلة

## 🚀 التشغيل

### 1. التثبيت
\```bash
pip install -r requirements.txt
\```

### 2. إعداد الـ API Key
\```bash
cp .env.example .env
# ثم عدّل .env وأضف OPENROUTER_API_KEY
\```

### 3. بناء قاعدة البيانات (مرة واحدة فقط)
\```bash
python ingest_database.py
\```

### 4. التشغيل
\```bash
python app_gradio.py
\```

## 📊 المتطلبات المُنجزة

| المطلوب | الحالة |
|---------|--------|
| Web Scraper + Vector DB | ✅ |
| RAG Pipeline بالعربية | ✅ |
| Chat Interface | ✅ |
| Article Citations | ✅ |
| Guardrails | ✅ |

## 🔒 الأمان
- ✅ Input validation
- ✅ Guardrails لمنع إساءة الاستخدام
- ✅ Disclaimer قانوني

## 📚 التوثيق
- [CONVERSATION_SUMMARY.md](CONVERSATION_SUMMARY.md) - ملخص كامل
- [PROJECT_ANALYSIS.md](PROJECT_ANALYSIS.md) - تحليل تقني
- [ARCHITECTURE_IMPROVEMENTS.md](ARCHITECTURE_IMPROVEMENTS.md) - تحسينات مستقبلية

## ⚠️ ملاحظات مهمة
- هذا MVP - نسخة أولية
- للـ production: راجع ARCHITECTURE_IMPROVEMENTS.md
- لا يُقدّم مشورة قانونية - فقط يعرض النصوص

## 📞 الدعم
- الموقع الرسمي: https://www.sfda.gov.sa
```

---

### Task 2: إنشاء HANDOVER.md (1 ساعة)

```markdown
# تسليم المشروع - Handover Document

## 📦 الملفات المُسلّمة

### الملفات الأساسية
- `app_gradio.py` - التطبيق الرئيسي
- `ingest_database.py` - بناء قاعدة البيانات
- `requirements.txt` - المكتبات المطلوبة
- `.env.example` - قالب الإعدادات

### قاعدة البيانات
- `chroma_db/` - Vector database (مُنشأة مسبقاً)
- `knowledge/` - الملفات المصدرية

### التوثيق
- `README.md` - دليل المستخدم
- `CONVERSATION_SUMMARY.md` - كل التفاصيل
- `PROJECT_ANALYSIS.md` - التحليل التقني

## ✅ ما تم إنجازه

### Week 6: ✅ Scraping & Indexing
- [x] Web scraping (JSON format)
- [x] Arabic text normalization
- [x] Vector DB setup (ChromaDB)
- [x] Multilingual embeddings (e5-large)

### Week 7: ✅ RAG Pipeline
- [x] Arabic-optimized embeddings
- [x] Chat UI (Gradio)
- [x] Article-by-article chunking
- [x] Source attribution

### Week 8: ✅ Guardrails
- [x] رفض أسئلة غير قانونية
- [x] رفض طلبات المشورة القانونية
- [x] Disclaimer في كل رد
- [x] Input validation

## 🚀 خطوات التشغيل

\```bash
# 1. التثبيت
pip install -r requirements.txt

# 2. إعداد API key
cp .env.example .env
nano .env  # أضف OPENROUTER_API_KEY

# 3. التشغيل
python app_gradio.py
\```

## 🧪 الاختبار

\```bash
# اختبارات أساسية
python test_basic.py

# اختبار يدوي
python app_gradio.py
# ثم جرّب الأسئلة من test cases
\```

## ⚠️ نقاط مهمة للمُراجع

### ✅ النقاط الإيجابية
- المتطلبات الأساسية كلها مُنجزة
- الكود منظم وموثق
- Guardrails قوية
- UI سهل الاستخدام

### ⚠️ للتطوير المستقبلي
(راجع ARCHITECTURE_IMPROVEMENTS.md)
- Caching للأداء
- Rate limiting
- Monitoring dashboard
- Agent integration للبحث في الموقع

## 📊 الإحصائيات

- **إجمالي الملفات:** 15+
- **عدد المستندات في DB:** (راجع ingest output)
- **Embedding Model:** multilingual-e5-large
- **LLM:** DeepSeek Chat (via OpenRouter)

## 🎓 للمراجعين

### السؤال: هل المشروع يفي بالمتطلبات؟

| المطلوب (من 01 - The Saudi Legal Assistant.md) | الحالة |
|------------------------------------------------|--------|
| Web Scraper | ✅ مُنجز |
| ChromaDB + Embeddings | ✅ مُنجز |
| Arabic semantic search | ✅ مُنجز |
| Chat interface | ✅ مُنجز |
| Article citations | ✅ مُنجز |
| Guardrails | ✅ مُنجز |

**الجواب: نعم ✅**

---

**تاريخ التسليم:** 2026-02-03
**الحالة:** MVP Ready for Review ✅
```

---

### Task 3: Final Checklist (1 ساعة)

```bash
# ✅ تأكد من:
- [ ] app_gradio.py يشتغل بدون أخطاء
- [ ] README.md محدّث وواضح
- [ ] requirements.txt كامل
- [ ] .env.example موجود
- [ ] chroma_db/ موجود ومبني
- [ ] test_basic.py يمر
- [ ] HANDOVER.md جاهز

# ✅ نظّف الملفات
rm -f *.pyc
rm -rf __pycache__

# ✅ Git commit (إذا مطلوب)
git add .
git commit -m "feat: MVP complete with guardrails and article citations"
git push
```

---

## 🎯 النتيجة النهائية

### ✅ ما سيكون جاهزاً بعد 3 أيام:

1. **MVP كامل** يفي بكل المتطلبات
2. **Article Citations** واضحة في كل إجابة
3. **Guardrails** قوية ضد إساءة الاستخدام
4. **Disclaimer** قانوني في كل رد
5. **Documentation** واضحة للتسليم
6. **Testing** أساسي مُنجز

### ⏳ ما لن يكون جاهزاً (يمكن تأجيله):

- Advanced caching
- Rate limiting
- Monitoring dashboard
- Agent integration (Playwright)

---

## 💡 نصيحة أخيرة

**ركّز على الأساسيات:**
- Day 1: الكود (Citations + Guardrails)
- Day 2: Testing (تأكد أنه يشتغل)
- Day 3: Documentation (تأكد أنه واضح)

**لا تضيّع وقت على:**
- Optimizations مبكرة
- Features إضافية
- Perfect code

**الهدف:** MVP يشتغل ✅

---

**Last Updated:** 2026-01-31
**Status:** Ready to Execute! 🚀
